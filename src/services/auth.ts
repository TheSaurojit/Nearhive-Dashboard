import { FirestoreService } from "@/firebase/firestoreService";

export async function getAdminByEmail(email: string) {

// export async function getAdminByEmail(email: string): Promise<{ id: string; email: string } | null> {
  // const result = await FirestoreService.getWhere("Authorized-Admins/document/admin", [
  //   ["email", "==", email],
  // ]);
  // return result.length ? result[0]  as { id: string; email: string } : null;
}
